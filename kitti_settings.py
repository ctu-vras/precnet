# Where the dataset is saved 
DATA_DIR = './datasets/'

# Where the model is saved
WEIGHTS_DIR = './model_data_kitti/'

# Where results (prediction plots and evaluation file) will be saved.
RESULTS_SAVE_DIR = './kitti_results/'
